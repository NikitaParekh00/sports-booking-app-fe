[build]
  command = "npm run build"
  # Optional: let the Next.js plugin manage output. If you must set it in the UI, use ".netlify/build".
  # publish = ".netlify/build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
